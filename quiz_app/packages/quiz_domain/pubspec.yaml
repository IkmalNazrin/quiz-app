name: quiz_domain
description: "Core business logic and entities for Quiz App"
version: 1.0.0
# publish_to: "none"

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  equatable: ^2.0.8
  uuid: ^4.5.1
  crypto: ^3.0.7
  collection: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.0
  lints: ^5.0.0
  mocktail: ^1.0.4

